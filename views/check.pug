<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title #{title}
  body
    header
    main
      .container
        form(action="")
          .input-container
            label(for="account") Введите лицевой счёт пользователя
            input(type="text" id="account" name="account" placeholder="Введите лицевой счёт пользователя")
          button(type="submit") Проверить
          if notFound
            p Не найден
    footer